<Window x:Class="MyCoinFlow.Views.TransaktionAuswahlDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Transaktion auswählen"
        Width="980"
        Height="650"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   FontSize="20"
                   FontWeight="SemiBold"
                   Text="Transaktion für Set auswählen"/>

        <DataGrid Grid.Row="2"
                  x:Name="Grid"
                  ItemsSource="{Binding Rows}"
                  SelectedItem="{Binding SelectedRow}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Datum" Binding="{Binding Datum}" Width="120"/>
                <DataGridTextColumn Header="Betrag" Binding="{Binding Betrag}" Width="120"/>
                <DataGridTextColumn Header="Adresse" Binding="{Binding AdresseName}" Width="2*"/>
                <DataGridTextColumn Header="Bank" Binding="{Binding BankName}" Width="*"/>
                <DataGridTextColumn Header="Notiz" Binding="{Binding Notiz}" Width="3*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Margin="0,0,10,0"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Content="Abbrechen"
                    Click="Abbrechen_Click"/>
            <Button Style="{StaticResource MaterialDesignRaisedButton}"
                    Content="Übernehmen"
                    Click="Uebernehmen_Click"/>
        </StackPanel>
    </Grid>
</Window>
