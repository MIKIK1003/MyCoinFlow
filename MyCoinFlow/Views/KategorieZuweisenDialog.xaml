<Window x:Class="MyCoinFlow.Views.KategorieZuweisenDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Kategorie zuordnen"
        Height="200" Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Händlerkategorie" Grid.Row="0" Grid.Column="0" Margin="0,0,10,10" VerticalAlignment="Center"/>
        <TextBox x:Name="KategorieBox" Grid.Row="0" Grid.Column="1" IsReadOnly="True"/>

        <TextBlock Text="Konto" Grid.Row="1" Grid.Column="0" Margin="0,0,10,10" VerticalAlignment="Center"/>
        <ComboBox x:Name="KontoCombo"
          Grid.Row="1" Grid.Column="1" Margin="0,0,0,10"
          DisplayMemberPath="Anzeige"
          SelectedValuePath="Id"/>


        <CheckBox x:Name="ChkSpeichern" Grid.Row="2" Grid.ColumnSpan="2"
                  Content="Mapping dauerhaft speichern (später)"
                  IsChecked="True"/>

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Abbrechen" IsCancel="True" Margin="0,0,10,0"/>
            <Button Content="OK" IsDefault="True" Click="Ok_Click"/>
        </StackPanel>
    </Grid>
</Window>
