<base:BaseWindow x:Class="MyCoinFlow.Views.AboTransaktionZuordnenDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
                 xmlns:base="clr-namespace:MyCoinFlow.UI.Base"
                 Title="Transaktion dem Abo zuordnen"
                 Width="1000"
                 Height="640"
                 WindowStartupLocation="CenterOwner">

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Suchleiste -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="SuchTextBox" Grid.Column="0"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     materialDesign:HintAssist.Hint="Suchtext (Adresse, Notiz)"
                     MinHeight="48"
                     KeyDown="SuchTextBox_KeyDown"/>

            <TextBox x:Name="BetragBox" Grid.Column="2"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     materialDesign:HintAssist.Hint="Betrag (±2%)"
                     MinHeight="48"
                     KeyDown="SuchTextBox_KeyDown"/>

            <DatePicker x:Name="VonPicker" Grid.Column="4"
                        materialDesign:HintAssist.Hint="von"
                        Style="{StaticResource MaterialDesignOutlinedDatePicker}"
                        MinHeight="48" Width="140"/>

            <DatePicker x:Name="BisPicker" Grid.Column="6"
                        materialDesign:HintAssist.Hint="bis"
                        Style="{StaticResource MaterialDesignOutlinedDatePicker}"
                        MinHeight="48" Width="140"/>

            <Button Grid.Column="8"
                    Style="{StaticResource IconTextButton}"
                    Tag="Magnify" Content="Suchen"
                    MinHeight="48" MinWidth="120"
                    Click="Suchen_Click"/>
        </Grid>

        <!-- Ergebnisliste (Mehrfachauswahl möglich) -->
        <DataGrid Grid.Row="2"
                  x:Name="ErgebnisGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  SelectionMode="Extended"
                  MouseDoubleClick="ErgebnisGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Datum" Binding="{Binding Datum, StringFormat=dd.MM.yyyy}" Width="110"/>
                <DataGridTextColumn Header="Transaktions-#" Binding="{Binding Id}" Width="110"/>
                <DataGridTextColumn Header="Betrag" Binding="{Binding Betrag, StringFormat={}{0:N2}}" Width="110">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Adresse" Binding="{Binding AdresseName}" Width="*"/>
                <DataGridTextColumn Header="Geldinstitut" Binding="{Binding BankName}" Width="160"/>
                <DataGridTextColumn Header="Notiz" Binding="{Binding Notiz}" Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock x:Name="HinweisText"
                       VerticalAlignment="Center"
                       Margin="0,0,16,0"
                       Foreground="{DynamicResource MaterialDesignBodyMediumBrush}"/>
            <Button Style="{StaticResource IconTextButton}"
                    Tag="Check" Content="Zuordnen"
                    MinWidth="130" Margin="0,0,10,0"
                    Click="Zuordnen_Click"/>
            <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                    Content="Abbrechen"
                    MinWidth="110"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</base:BaseWindow>
