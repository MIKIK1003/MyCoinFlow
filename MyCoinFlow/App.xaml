<Application x:Class="MyCoinFlow.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Material Design Theme -->
                <materialDesign:BundledTheme BaseTheme="Light"
                                             PrimaryColor="DeepPurple"
                                             SecondaryColor="Lime" />
                <!-- Material Design 2 Defaults -->
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign2.Defaults.xaml" />
                <!-- Kompatibilität für alte Styles -->
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.ObsoleteBrushes.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.ObsoleteStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- ========================================= -->
            <!-- Global: einheitliche PackIcon-Darstellung -->
            <!-- ========================================= -->
            <Style TargetType="materialDesign:PackIcon" x:Key="DefaultIconStyle">
                <Setter Property="Width" Value="20"/>
                <Setter Property="Height" Value="20"/>
                <Setter Property="Margin" Value="0,0,8,0"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>

            <!-- ===================================================== -->
            <!-- Reusable: Button mit Icon (aus Button.Tag) + Text     -->
            <!-- Basierend auf MaterialDesignRaisedButton              -->
            <!-- Verwendung:                                           -->
            <!--   <Button Style="{StaticResource IconTextButton}"     -->
            <!--           Tag="TimerPlus" Content="Zeitraum hinzufügen" .../> -->
            <!-- ===================================================== -->
            <Style x:Key="IconTextButton"
                   TargetType="Button"
                   BasedOn="{StaticResource MaterialDesignRaisedButton}">
                <Setter Property="Padding" Value="12,8"/>
                <Setter Property="ContentTemplate">
                    <Setter.Value>
                        <DataTemplate>
                            <DockPanel LastChildFill="True" VerticalAlignment="Center">
                                <materialDesign:PackIcon Style="{StaticResource DefaultIconStyle}"
                                                         Kind="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Tag}"
                                                         DockPanel.Dock="Left"/>
                                <TextBlock Text="{Binding}" VerticalAlignment="Center"/>
                            </DockPanel>
                        </DataTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Variante für "gefährliche" Aktionen (z. B. Löschen) -->
            <Style x:Key="IconTextButton.Danger"
                   TargetType="Button"
                   BasedOn="{StaticResource IconTextButton}">
                <Setter Property="Background" Value="{DynamicResource MaterialDesignValidationErrorBrush}"/>
                <Setter Property="Foreground" Value="White"/>
            </Style>

            <!-- Optional: Mehr Platz rechts in allen DataGrid-Headern,
                 damit Sortierpfeile (Adorner) nicht am Rand kleben. -->
            <Style TargetType="DataGridColumnHeader">
                <Setter Property="Padding" Value="8,0,20,0"/>
                <Setter Property="Background" Value="{DynamicResource MaterialDesignPaper}"/>
                <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
            </Style>


            <!-- Einheitliche Icon-Optik -->
            <Style x:Key="DefaultIconStyle.Nav" TargetType="materialDesign:PackIcon">
                <Setter Property="Width" Value="35"/>
                <Setter Property="Height" Value="35"/>
                <Setter Property="Margin" Value="0,0,10,0"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>

            <!-- Nav-Button: Icon links, Text rechts, linksbündig, Flat-Stil -->
            <Style x:Key="NavIconButton" TargetType="Button" BasedOn="{StaticResource MaterialDesignFlatButton}">
                <Setter Property="Padding" Value="15,0"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                <Setter Property="FontSize" Value="20"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
                <Setter Property="ContentTemplate">
                    <Setter.Value>
                        <DataTemplate>
                            <DockPanel LastChildFill="True" VerticalAlignment="Center">
                                <!-- Icon-Name kommt aus Button.Tag -->
                                <materialDesign:PackIcon DockPanel.Dock="Left"
                                   Style="{StaticResource DefaultIconStyle.Nav}"
                                   Kind="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Tag}"/>
                                <!-- Text kommt aus Button.Content -->
                                <TextBlock Text="{Binding}" VerticalAlignment="Center"/>
                            </DockPanel>
                        </DataTemplate>
                    </Setter.Value>
                </Setter>
                <!-- Hover/Pressed Feedback -->
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{DynamicResource MaterialDesignSelection}"/>
                    </Trigger>
                    <Trigger Property="IsPressed" Value="True">
                        <Setter Property="Background" Value="{DynamicResource MaterialDesignSelection}"/>
                        <Setter Property="Opacity" Value="0.9"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
            
        </ResourceDictionary>
    </Application.Resources>
     
    
</Application>