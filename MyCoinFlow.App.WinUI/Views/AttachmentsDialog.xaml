<?xml version="1.0" encoding="utf-8"?>
<window:PersistentWindow x:Class="MyCoinFlow.WinUI.Views.AttachmentsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:window="using:MyCoinFlow.WinUI">
    <Grid x:Name="RootGrid" Padding="24,20" RowSpacing="14" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Spacing="3">
            <TextBlock Text="Anhänge zur Transaktion" FontSize="24" FontWeight="SemiBold"/>
            <TextBlock x:Name="SubtitleText" TextWrapping="Wrap" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>
        <InfoBar x:Name="MessageBar" Grid.Row="1" IsOpen="False" IsClosable="True"/>
        <Border Grid.Row="2" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
            <Grid>
                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                <Grid Padding="14,10" Background="{ThemeResource SubtleFillColorSecondaryBrush}" ColumnSpacing="10">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="100"/><ColumnDefinition Width="110"/><ColumnDefinition Width="250"/></Grid.ColumnDefinitions>
                    <TextBlock Text="DATEI" FontSize="11" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="1" Text="GRÖSSE" FontSize="11" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="2" Text="STATUS" FontSize="11" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="3" Text="AKTIONEN" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                </Grid>
                <ListView x:Name="FilesList" Grid.Row="1" SelectionMode="Single">
                    <ListView.ItemContainerStyle><Style TargetType="ListViewItem"><Setter Property="HorizontalContentAlignment" Value="Stretch"/><Setter Property="Padding" Value="0"/></Style></ListView.ItemContainerStyle>
                    <ListView.ItemTemplate><DataTemplate>
                        <Grid Padding="14,11" ColumnSpacing="10" MinHeight="58">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="100"/><ColumnDefinition Width="110"/><ColumnDefinition Width="250"/></Grid.ColumnDefinitions>
                            <StackPanel><TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/><TextBlock Text="{Binding FileName}" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/></StackPanel>
                            <TextBlock Grid.Column="1" Text="{Binding SizeText}" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="2" Text="{Binding OcrStatus}" VerticalAlignment="Center"/>
                            <StackPanel Grid.Column="3" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
                                <Button Content="Zurückstellen" Click="OnUnlinkClick"/>
                                <Button Content="Löschen" Click="OnDeleteClick"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate></ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>
        <Button Grid.Row="3" Content="Schließen" HorizontalAlignment="Right" Style="{StaticResource AccentButtonStyle}" Click="OnCloseClick"/>
    </Grid>
</window:PersistentWindow>
