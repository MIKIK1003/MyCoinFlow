<?xml version="1.0" encoding="utf-8"?>
<ContentDialog x:Class="MyCoinFlow.WinUI.Views.DocumentLinkDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Dokument verknüpfen" PrimaryButtonText="Verknüpfen" SecondaryButtonText="Datei auswählen" CloseButtonText="Abbrechen" DefaultButton="Primary" MinWidth="900"
    PrimaryButtonClick="OnPrimaryButtonClick">
    <ContentDialog.Resources><x:Double x:Key="ContentDialogMaxWidth">1040</x:Double></ContentDialog.Resources>
    <Grid RowSpacing="12">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="390"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <TextBlock Text="Freies DMS-Dokument auswählen oder eine neue Datei vom Computer verknüpfen." Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        <Grid Grid.Row="1" ColumnSpacing="8"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <TextBox x:Name="SearchBox" PlaceholderText="Dateiname, Titel, Kategorie oder Inhalt suchen" KeyDown="OnSearchKeyDown"/>
            <Button Grid.Column="1" Content="Suchen" Click="OnSearchClick"/>
        </Grid>
        <ListView x:Name="DocumentsList" Grid.Row="2" SelectionMode="Single" DoubleTapped="OnDoubleTapped">
            <ListView.ItemContainerStyle><Style TargetType="ListViewItem"><Setter Property="HorizontalContentAlignment" Value="Stretch"/><Setter Property="Padding" Value="0"/></Style></ListView.ItemContainerStyle>
            <ListView.ItemTemplate><DataTemplate><Grid Padding="12,10" ColumnSpacing="12" MinHeight="58">
                <Grid.ColumnDefinitions><ColumnDefinition Width="1.2*"/><ColumnDefinition Width="1.2*"/><ColumnDefinition Width="130"/><ColumnDefinition Width="110"/><ColumnDefinition Width="90"/></Grid.ColumnDefinitions>
                <StackPanel><TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/><TextBlock Text="{Binding FileName}" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/></StackPanel>
                <TextBlock Grid.Column="1" Text="{Binding Description}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="2" Text="{Binding Category}" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="3" Text="{Binding DateText}" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="4" VerticalAlignment="Center"><TextBlock Text="{Binding AmountText}" HorizontalAlignment="Right"/><TextBlock Text="{Binding SizeText}" FontSize="11" HorizontalAlignment="Right" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/></StackPanel>
            </Grid></DataTemplate></ListView.ItemTemplate>
        </ListView>
        <InfoBar x:Name="MessageBar" Grid.Row="3" IsOpen="False" IsClosable="True"/>
    </Grid>
</ContentDialog>
