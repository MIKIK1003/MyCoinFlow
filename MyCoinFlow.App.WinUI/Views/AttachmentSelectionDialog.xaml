<?xml version="1.0" encoding="utf-8"?>
<ContentDialog x:Class="MyCoinFlow.WinUI.Views.AttachmentSelectionDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Dokument öffnen"
    CloseButtonText="Schließen"
    DefaultButton="Close"
    MinWidth="680">
    <ContentDialog.Resources>
        <x:Double x:Key="ContentDialogMaxWidth">760</x:Double>
    </ContentDialog.Resources>
    <Grid RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="320"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Mehrere Dokumente sind verknüpft. Ein Dokument anklicken – es wird sofort im Standardprogramm geöffnet."
                   TextWrapping="Wrap" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        <InfoBar x:Name="MessageBar" Grid.Row="1" IsOpen="False" IsClosable="True"/>
        <ListView x:Name="FilesList" Grid.Row="2" SelectionMode="Single" IsItemClickEnabled="True" ItemClick="OnItemClick">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="14,11" ColumnSpacing="12" MinHeight="58">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="36"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="90"/>
                            <ColumnDefinition Width="90"/>
                            <ColumnDefinition Width="32"/>
                        </Grid.ColumnDefinitions>
                        <FontIcon Glyph="&#xEA90;" FontSize="20" Foreground="{StaticResource MyCoinFlowAccentBrush}" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{Binding FileName}" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>
                        <TextBlock Grid.Column="2" Text="{Binding SizeText}" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="3" Text="{Binding OcrStatus}" VerticalAlignment="Center"/>
                        <FontIcon Grid.Column="4" Glyph="&#xE8E5;" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</ContentDialog>
