# MyCoinFlow 3.1.1.0

- Status: in Arbeit
- Basisversion: 3.1.0.0
- Basiscommit: 3f4a4de
- Start: 2026-09-02
- Versionszweig: master
- Versionsziel: Das neue WinUI-Modul Fakturieren fachlich und technisch sauber abgrenzen
  und anschließend in klar getrennten Arbeitspaketen umsetzen. ShopFlow Debitoren dient als
  Masterreferenz für Hierarchie und Bediensemantik; Fachmodell und Implementierung bleiben
  MyCoinFlow-spezifisch.

Der Basiscommit `3f4a4de` ist der auf `origin/master` veröffentlichte Abschluss von
3.1.0.0 AP01. Der Arbeitsbaum war beim Versionsstart sauber. Produktänderungen dieser Version
bleiben auf `MyCoinFlow.App.WinUI/` begrenzt; begleitend werden nur die Versions- und
Projektverweise unter `DOCS/` und im Repository-README geführt. Die frühere WPF-Oberfläche
und der referenzierte Fachkern unter `MyCoinFlow/` werden nicht geändert. ShopFlow bleibt
eine schreibgeschützte Referenz.

## Startprüfung

- `master`, `origin/master` und `origin/HEAD` standen auf Commit `3f4a4de` mit dem
  Abschlusscommit `v3.1.0.0 AP01: Transaktionen angleichen`; der Arbeitsbaum war sauber.
- Das abgeschlossene Arbeitspaket 3.1.0.0 AP01 ist im vorherigen Versionsblatt einschließlich
  Commit, Push, Ergebnis und verbleibender Live-Nachabnahme dokumentiert. Der noch auf
  `in Arbeit` stehende Kopfstatus wurde beim Start dieser Version auf `abgeschlossen`
  berichtigt.
- `MyCoinFlow.App.WinUI/MyCoinFlow.App.WinUI.csproj` stand auf 3.1.0.0. Für das eigenständige
  neue Featurepaket innerhalb der aktiven Produktlinie 3.1.x wird die Version 3.1.1.0
  verwendet; ein vierter Stellenzähler wäre für den neuen Modulgegenstand zu klein.
- Die konkrete Fakturieren-Fachlichkeit, Abläufe, Datenquellen und der gewünschte Umfang der
  Debitoren-Übertragung sind noch nicht spezifiziert. Deshalb beginnt AP01 in Klärung und
  enthält keine vorweggenommene Implementierung.

## Versionsstart-Prüfungen

- Die Projektdatei wurde als XML gelesen; `Version`, `AssemblyVersion` und `FileVersion`
  stehen einheitlich auf 3.1.1.0.
- `dotnet build MyCoinFlow.App.WinUI\MyCoinFlow.App.WinUI.csproj -c Debug --no-restore`:
  erfolgreich mit 0 Fehlern. Die 25 Warnungen liegen vollständig im unveränderten,
  referenzierten Projekt `MyCoinFlow/`, darunter drei bestehende NU1701-Hinweise.
- `git diff --check`: erfolgreich; nur die unter Windows erwarteten LF-/CRLF-Hinweise.
- Der Versionsstart-Diff ist auf WinUI-Projektversion, Versionsblätter und den aktiven
  README-Verweis begrenzt. Es wurde keine Fakturieren-Fach- oder UI-Implementierung begonnen.

## Arbeitspaket 01 · Fakturieren abgrenzen

- Tasktitel: 3.1.1.0 · 01 · Fakturieren abgrenzen
- Status: abgeschlossen
- Ziel: Die noch folgenden Zielinformationen des Benutzers in einen bestätigten,
  umsetzbaren Umfang für das neue WinUI-Modul Fakturieren überführen. Dokumentiert werden
  Anwendungsfälle, Daten- und Mandantengrenzen, Seiten und Dialoge, Aktionen, Zustände,
  ShopFlow-Debitoren-Referenzen, Wiederverwendung vorhandener WinUI-Bausteine,
  Akzeptanzkriterien und die sinnvolle Aufteilung der Umsetzungspakete.
- Nicht-Ziele: Fakturieren vor der Zielbestätigung implementieren; ShopFlow-Code,
  Fachmodelle, Services oder Datenzugriffe kopieren; Datenbankschema, Buchungs-, Lizenz-,
  Update- oder Revisionslogik ändern; `MyCoinFlow/`, ShopFlow oder nicht benötigte
  WinUI-Module bearbeiten; die gesamte Anwendung nebenbei neu gestalten.
- Ausgangsstand: sauberer, mit `origin/master` synchroner Commit `3f4a4de` auf `master`;
  die WinUI-Projektversion und der aktive Versionsverweis werden beim Versionsstart auf
  3.1.1.0 gesetzt.
- Betroffene Module: schreibend zunächst nur Versionsführung und
  `MyCoinFlow.App.WinUI/MyCoinFlow.App.WinUI.csproj`; die für Fakturieren betroffenen
  WinUI-Views, ViewModels, Repositories und Services werden erst nach den Zielinformationen
  verbindlich benannt. ShopFlow Debitoren darf nur lesend als Referenz geprüft werden.
- Maßgebliche Quellen: `AGENTS.md`, `MyCoinFlow.App.WinUI/AGENTS.md`,
  `DOCS/PROJECT_CONTRACT.md`, `DOCS/ARCHITECTURE.md`, `DOCS/UX_DESIGN_SYSTEM.md`,
  `DOCS/UI_ACTION_CATALOG.md`, `DOCS/UI_CONTRACT_BASELINE.md`, `DOCS/VERSIONING.md`,
  der aktuelle WinUI-Code und die nachfolgenden Zielinformationen des Benutzers.

### Akzeptanzkriterien

- [x] Die gewünschten Fakturieren-Anwendungsfälle und bewusst ausgeschlossenen Abläufe sind
      bestätigt und im Versionsblatt dokumentiert.
- [x] Betroffene WinUI-Seiten, Dialoge, ViewModels, Datenzugriffe und vorhandene
      Wiederverwendungsbausteine sind benannt; Schreibänderungen bleiben auf
      `MyCoinFlow.App.WinUI/` und begleitende Versionsdokumentation begrenzt.
- [x] Für jede übernommene ShopFlow-Debitoren-Struktur ist festgelegt, welche Hierarchie,
      Aktions- oder Zustandssemantik MyCoinFlow übernimmt und welche ShopFlow-Fachlichkeit
      ausdrücklich nicht übernommen wird.
- [x] Mandantenbezug, Finanz- und Revisionsgrenzen sowie Lizenzierung sind entweder als
      unverändert bestätigt oder als ausdrücklich zu klärender Punkt sichtbar.
- [x] Die Umsetzung ist in eigenständig abschließbare Folgearbeitspakete mit jeweils einem
      beobachtbaren Ergebnis, Akzeptanzkriterien und geplanten Prüfungen zerlegt.
- [x] Der Abschlussdiff von AP01 enthält keine vorweggenommene Fach- oder UI-Implementierung.

### Entscheidungen und Umsetzung

- Version 3.1.1.0 bleibt innerhalb der aktiven WinUI-Produktlinie 3.1.x und reserviert ein
  eigenständiges Featurepaket für Fakturieren.
- AP01 ist ausschließlich ein Klärungs- und Abgrenzungspaket. Die Implementierung beginnt
  erst nach bestätigtem Ziel in einem eigenen Folgearbeitspaket.
- „Klon von Debitoren“ bezeichnet bis zur fachlichen Bestätigung die erkennbare Übernahme
  von Hierarchie, Gruppierung, Such-/Status-/Filterstruktur, Aktionssemantik und Zuständen.
  Eine Übernahme von ShopFlow-Fachmodell, Services, Datenzugriffen oder ganzen Seiten ist
  nicht freigegeben.
- Der Benutzer hat am 2. September 2026 den ganzen Dokumentfluss, artikelabhängige
  Ertragskonten einschließlich Neuanlage, Fremdwährungen, einen Finanzen-Einstellungsbereich
  sowie die erweiterten Funktionen E-Mail, DMS-Beilagen, Mahnwesen, Skonto,
  Rabatt/Rundung und Abzahlungsplan bestätigt.

### Bestätigter Fachumfang

- Fakturieren wird ein eigener Hauptnavigationspunkt im Bereich Finanzen der bestehenden
  WinUI-Shell. Das Modul arbeitet in der beim Login gewählten Datenbank; sie bleibt die
  Mandantengrenze. Normale Fakturierungsarbeit steht angemeldeten Benutzern zur Verfügung,
  finanzielle Stammdaten und Einstellungen sind administrativ geschützt. Es wird kein
  externes Fibu-, ABACUS- oder ERP-Berechtigungsmodell übernommen.
- Der ganze Dokumentfluss umfasst Offerte, Auftragsbestätigung, Lieferung, Rechnung,
  Teil- und Schlussrechnung sowie Korrektur- beziehungsweise Stornobeleg. Lieferung ist in
  MyCoinFlow ein Dokument- und Workflowzustand ohne Lager-, Seriennummern-, Einkaufs-,
  Fertigungs-, Reparatur- oder Vermietungsbewegung.
- Dokumente führen Entwurf, Versand-/Fortschrittszustände und eine sichtbare Dokumentfolge.
  Definitive finanzwirksame Belege werden nicht physisch gelöscht oder still überschrieben.
  Nummer, Empfänger, Positionen, Texte, Preise, Steuern, Währung, Kurs, Kontierung,
  Zahlungsziel und Zahlungsreferenz werden als Snapshot eingefroren; spätere Änderungen
  erfolgen nachvollziehbar über Folge- oder Korrekturbelege.
- Bestehende MyCoinFlow-Adressen und Konten werden referenziert und nicht dupliziert.
  Fakturierungsspezifische Adressdaten wie Sprache, Rechnungs-E-Mail, Zahlungsziel und
  abweichende Rechnungs- oder Lieferanschrift liegen in einem ergänzenden Profil innerhalb
  des WinUI-Moduls.
- Innerhalb von Fakturieren entsteht ein Artikel-/Leistungsstamm mit mindestens manueller
  Artikelnummer, Bezeichnung, Beschreibung, Einheit, Kategorie, Aktivstatus, Verkaufspreis,
  Texten und artikelabhängigem Standard-Ertragskonto. Artikelnummern werden vor dem Speichern
  normalisiert und ohne Beachtung der Groß-/Kleinschreibung gegen aktive und inaktive
  Datensätze geprüft; ein eindeutiger Datenbankindex verhindert parallele Dubletten.
- Ertragskonten dürfen aus der Artikelerfassung beziehungsweise den Finanzen-Einstellungen
  heraus über den vorhandenen MyCoinFlow-Kontenplan neu angelegt und anschließend gewählt
  werden. Die Funktion verwendet den gemeinsamen Kontoeditor und erzeugt keinen parallelen
  Kontenstamm. Die Ertragskontenzuordnung wird je Dokumentposition eingefroren.
- Der ShopFlow-Position-Composer wird als MyCoinFlow-WinUI-Baustein mit Artikel/Leistung,
  freier Text-/Abschnittsposition, Artikelsuche, überschreibbarer Bezeichnung, Kategorie,
  Menge, Preis, Textbaustein, formatiertem Haupt- und Zusatztext, Fusstext, sofortiger
  Mehrfacherfassung, Umordnen und Tastaturablauf umgesetzt. Seriennummernauswahl und
  Bestandswirkung entfallen.
- Der formatierte Texteditor umfasst mit nativen WinUI-Mitteln Schriftart und -grösse,
  fett, kursiv, unterstrichen, Aufzählung, Ausschneiden/Kopieren/Einfügen, definierte
  Textfarben, Textbausteine sowie Klartext- und Formatsnapshot.
- Zahlungsziel, Rabatt, Rundung, Skonto, Abzahlungsplan, Teilzahlungen, Überzahlungen,
  Klärfälle, Mahnstatus, DMS-Beilagen und E-Mail-Versand gehören zum bestätigten Umfang.
  Fehler- und Wiederholungszustände bleiben sichtbar; finanzwirksame Vorgänge erhalten eine
  nachvollziehbare Ereignis- und Verknüpfungshistorie.

### Finanz- und Zahlungsmodell

- Der Finanzen-Einstellungsbereich enthält mandantenbezogen Aussteller- und
  Rechnungsstammdaten, Nummernkreise, Basiswährung, aktivierte Dokumentwährungen,
  Wechselkurse und deren Gültigkeit, MWST-Konfiguration, Zahlungs- und QR-Konten,
  Ertragskonten, Kursgewinn-/Kursverlustkonten sowie E-Mail-Versanddaten. Geheimnisse werden
  nicht ungeschützt in der Mandantendatenbank gespeichert.
- Rechnungen dürfen jede in den Einstellungen aktivierte Währung verwenden. Betrag,
  Währung, Umrechnungskurs, Kursdatum und Kursquelle werden beim Definitivsetzen und bei der
  Zahlung gespeichert. Die MyCoinFlow-Transaktion wird in der Basiswährung gebucht; der
  Originalbetrag bleibt in der Fakturierungsverknüpfung erhalten. Kursdifferenzen werden
  über die konfigurierten Konten nachvollziehbar ausgewiesen.
- Die Schweizer QR-Rechnung unterstützt normgemäss CHF und EUR. Nur für diese Währungen
  wird ein Swiss-QR-Zahlteil erzeugt; andere aktivierte Rechnungswährungen erhalten
  vollständige Zahlungsangaben ohne Swiss-QR-Code und einen klaren Hinweis in Vorschau und
  Ausgabe.
- Die QR-/ISO-Referenz ist der primäre stabile Schlüssel zwischen definitiver Rechnung,
  CAMT-Zeile und Zahlung. Betrag, Währung und Adresse dienen der Plausibilisierung.
  Mehrdeutige, falsche oder unvollständige Treffer werden nicht automatisch geschlossen,
  sondern einem sichtbaren Klärbestand zugewiesen.
- Eine vollständig zugeordnete CAMT-Zahlung wird über den vorhandenen Buchungstyp
  Adresse → Bank definitiv verbucht. Enthält die Rechnung Positionen mit mehreren
  Ertragskonten, wird der Zahlungseingang nach den eingefrorenen Positionsanteilen je
  Ertragskonto aggregiert und in mehrere gemeinsam verknüpfte Transaktionen aufgeteilt.
  Die Summe der Teiltransaktionen entspricht exakt dem in Basiswährung verbuchten
  Zahlungseingang; Rundungs- und Kursdifferenzen werden explizit zugeordnet.
- Offene Rechnungen zeigen mindestens Offen, Überfällig, Teilbezahlt, Klärung, Bezahlt und
  Korrigiert. Erst eine vollständig und widerspruchsfrei zugeordnete Zahlung schliesst den
  offenen Status. Teil-, Über- und Fehlzahlungen bleiben mit Restbetrag und Ursache sichtbar.

### Übernahme aus ShopFlow Debitoren

- Übernommen werden die erkennbare Seitenhierarchie, gruppierte Funktionsleiste,
  Such-/Status-/Schnellansicht-/Filterstruktur, Vorgangsliste, Dokumentfolge, sichtbarer
  Auswahlkontext, Positionsarbeit, Statussemantik und die bestätigten Benutzerabläufe.
- ShopFlow-Code, Entity-Framework-Modell, Repositories, Fibu-Outbox, ABACUS-Synchronisation,
  externer Debitorenstamm, Providerabrechnung sowie Lager- und Seriennummernlogik werden
  nicht kopiert. Funktionsparität entsteht durch eine MyCoinFlow-spezifische Umsetzung in
  Models, Data-Repositories, Services, ViewModels und Views unter
  `MyCoinFlow.App.WinUI/`.
- Wiederverwendet werden die bestehenden MyCoinFlow-Adressen, der Kontenplan,
  Adresse-zu-Bank-Transaktionen, CAMT-Staging, DMS-Anhänge, Login-/Datenbankkontext,
  PersistentWindow, Fensterzustandsdienst sowie zentrale Design- und Aktionsressourcen.
  Notwendige Erweiterungen an bestehenden WinUI-Seiten bleiben auf die Integration von
  Fakturieren begrenzt.

### Verbindliche Folgearbeitspakete

- AP02 · Fakturieren-Grundlage und Finanzen einstellen: Navigation und Modulshell,
  WinUI-eigenes Schema, Repositories, Rollen-/Mandantengrenze sowie Aussteller,
  Nummernkreise, Basis-/Dokumentwährungen, Kurse, MWST, Zahlungs-, QR-, Ertrags- und
  Kursdifferenzkonten. Ergebnis ist ein leerzustandsfähiges Modul mit vollständig
  persistierten und validierten Finanzstammdaten.
- AP03 · Artikel und fakturierbare Objekte: Artikel-/Leistungsverwaltung, manuelle eindeutige
  Artikelnummer, Dublettenprüfung mit Datenbankgarantie, Texte, Preise, Kategorien,
  artikelabhängige Ertragskonten sowie direkte und immobilienbezogene fakturierbare Objekte
  mit rechtssicheren Empfänger-Defaults. Ergebnis sind belastbare, durchsuchbare
  Fakturierungsstammdaten und auswählbare Objekt-/Empfängerkontexte.
- AP04 · Positionen verfassen: gemeinsamer formatierter Texteditor, Textbausteine,
  Position-Composer, Einzelbearbeitung, Reihenfolge, Fusstexte und Snapshots. Ergebnis ist
  die vollständig prüfbare Positionserfassung unabhängig vom Dokumentworkflow.
- AP05 · Dokumentfluss führen: Offerte, Auftragsbestätigung, Lieferung und Übernahme in
  Rechnung mit Dokumentfolge, Kopf-/Adresssnapshots, Suche, Status, Filter und erlaubten
  Übergängen ohne Lagerwirkung. Ergebnis ist der vollständige nicht finanzielle
  Verkaufsdokumentfluss.
- AP06 · Rechnungen definitiv führen: Rechnung, Teil-/Schlussrechnung, Korrektur/Storno,
  Rabatt, Rundung, Skonto, Zahlungsziel, Abzahlungsplan, Definitivsetzung,
  Revisionsereignisse und offene Posten. Ergebnis ist ein unveränderlicher und
  nachvollziehbarer Rechnungsbestand vor Zahlung.
- AP07 · Dokumente als PDF und QR ausgeben: reproduzierbare PDFs für die bestätigte
  Dokumentkette, Rechnungsvorschau, Swiss-QR-Zahlteil für CHF/EUR und alternative
  Zahlungsangaben für andere Währungen. Ergebnis sind visuell und inhaltlich geprüfte
  Ausgabedokumente.
- AP08 · CAMT-Zahlungen zuordnen: Referenzabgleich, Fremdwährungsumrechnung,
  Ertragskontenaufteilung, verknüpfte Adresse-zu-Bank-Transaktionen, Teil-/Überzahlung,
  Klärbestand, Mahnstatus und Statusabschluss. Ergebnis ist der geschlossene interne
  Rechnung-Zahlung-Transaktionskreislauf ohne Fibu-Übergabe.
- AP09 · E-Mail, DMS und Gesamtabnahme: PDF-Versand, DMS-Beilagen und Ablage,
  Fehler-/Wiederholungsnachweise sowie Ende-zu-Ende-Prüfung des gesamten Dokument- und
  Zahlungsflusses. Ergebnis ist der betriebsfähige Gesamtworkflow mit dokumentierter
  manueller UI- und PDF-Abnahme.

### Geplante Prüfungen

- Zielinformationen gegen Projekt-, Architektur-, UX- und Versionsvertrag prüfen.
- Aktuellen MyCoinFlow-WinUI-Code und ShopFlow Debitoren gezielt für die bestätigten
  Anwendungsfälle vergleichen.
- Geplante Folgepakete auf jeweils ein Ergebnis, klare Nicht-Ziele und überprüfbare
  Akzeptanzkriterien prüfen.
- Vor Abschluss `git diff --check`, vollständigen Scope-Diff und Git-Status prüfen.

### Abschluss

- Commit: gemeinsam im nachfolgenden AP02-Abschlusscommit
  `v3.1.1.0 AP02: Fakturieren-Grundlage und Finanzen einstellen`
- Push: gemeinsam mit AP02 nach `origin/master`
- Ergebnis: Der vollständige Fakturieren-Dokumentfluss, seine internen Finanz- und
  CAMT-Grenzen, Stammdaten, ShopFlow-Referenzen und Folgearbeitspakete sind bestätigt.
  AP01 enthält keine Fach- oder UI-Implementierung.
- Offene Punkte: keine fachlichen Scope-Blocker; Detailentscheidungen innerhalb der
  bestätigten Grenzen werden in den jeweiligen Folgearbeitspaketen anhand des aktuellen
  Codes getroffen.

## Arbeitspaket 02 · Fakturieren-Grundlage und Finanzen einstellen

- Tasktitel: 3.1.1.0 · 02 · Fakturieren-Grundlage und Finanzen einstellen
- Status: abgeschlossen
- Ziel: Das neue WinUI-Modul Fakturieren als eigenen Hauptnavigationspunkt im Finanzbereich
  mit einer belastbaren, leerzustandsfähigen Arbeitsbereichsshell im
  ShopFlow-Debitoren-Zielbild bereitstellen. Ein WinUI-eigenes, idempotent initialisiertes
  SQL-Server-Schema und getrennte Repositories schaffen die mandantenbezogene Grundlage der
  Folgepakete. Der administrativ geschützte Einstellungsbereich Finanzen persistiert und
  validiert Aussteller- und Rechnungsstammdaten, Dokumentnummernkreise, Basis- und aktivierte
  Dokumentwährungen, manuelle Wechselkurse mit Gültigkeit und Quelle, MWST-Sätze,
  Zahlungs-/QR-Konten mit Referenz auf bestehende Geldinstitute, zulässige Ertragskonten
  sowie Kursgewinn- und Kursverlustkonten.
- Nicht-Ziele: Artikel- oder Leistungsstamm, Artikelnummern und Dublettenprüfung,
  Textbausteine oder Position-Composer; Dokumenterfassung und -workflow; definitive
  Rechnungen, Zahlungen, CAMT-Zuordnung, PDF-/Swiss-QR-Erzeugung, alternative
  Zahlungsangaben, E-Mail-Versand oder DMS-Verknüpfung; Kopieren von ShopFlow-Fachmodell,
  Services, Datenzugriffen oder ganzen Seiten; parallele Adress- oder Kontenstämme;
  Änderungen unter `MyCoinFlow/` oder `C:\DEV\ShopFlow`.
- Ausgangsstand: Basiscommit `3f4a4de` auf `master`. Die noch uncommittierten
  Versionsstart-/AP01-Änderungen an `DOCS/VERSIONS/3.1.0.0.md`,
  `DOCS/VERSIONS/3.1.1.0.md`, `MyCoinFlow.App.WinUI/MyCoinFlow.App.WinUI.csproj` und
  `README.md` sind geschützter Bestand und werden nicht AP02 zugerechnet.
- Betroffene Module: `MyCoinFlow.App.WinUI/MainWindow.*` für Navigation;
  `MyCoinFlow.App.WinUI/Views/InvoicingPage.*` und
  `MyCoinFlow.App.WinUI/ViewModels/InvoicingViewModel.cs` für die Modulshell;
  `MyCoinFlow.App.WinUI/Views/SettingsPage.*` und
  `MyCoinFlow.App.WinUI/Views/FinanceSettingsControl.*` für den Adminbereich;
  `MyCoinFlow.App.WinUI/Models/InvoicingModels.cs`,
  `MyCoinFlow.App.WinUI/Data/InvoicingSchema.cs`,
  `MyCoinFlow.App.WinUI/Data/InvoicingWorkspaceRepository.cs` und
  `MyCoinFlow.App.WinUI/Data/FinanceSettingsRepository.cs` für Modelle, Schema und
  Datenzugriffe; bei nachgewiesener Wiederverwendung nur gezielte semantische Ressourcen in
  `MyCoinFlow.App.WinUI/App.xaml`.
- Mandanten- und Rollengrenze: Jede Fakturieren-Verbindung wird neu aus
  `MyCoinFlow.WinUI.Data.ConnectionStrings.Current` geöffnet und gegen die beim Login aktive
  Datenbank geprüft; es gibt keinen globalen oder datenbankübergreifenden Fakturierungsstamm.
  Angemeldete Benutzer dürfen die Modulshell lesen und später normale Fakturierungsarbeit
  ausführen. Finanzstammdaten sind in Navigation, Oberfläche und schreibendem Repository über
  `CurrentUserContext.IsAdmin` geschützt.
- ShopFlow-Referenz: Debitoren liefert ausschließlich Seitenkopf, gruppierte und horizontal
  scrollbare Funktionsleiste, Such-/Status-/Schnellansicht-/Filterreihenfolge,
  Listen-/Detailhierarchie, sichtbaren Auswahlkontext sowie Leer-, Fehler- und deaktivierte
  Zustände. ShopFlow-Einstellungen liefern nur die Gliederung dichter Finanzstammdaten.
  MyCoinFlow verwendet eigene WinUI-Ressourcen, SQL-Tabellen, Modelle und Repositories.
- Maßgebliche Quellen: `AGENTS.md`, `MyCoinFlow.App.WinUI/AGENTS.md`,
  `DOCS/PROJECT_CONTRACT.md`, `DOCS/ARCHITECTURE.md`, `DOCS/UX_DESIGN_SYSTEM.md`,
  `DOCS/UI_ACTION_CATALOG.md`, `DOCS/UI_CONTRACT_BASELINE.md`, `DOCS/VERSIONING.md`,
  AP01 dieses Versionsblatts, die aktuelle WinUI-Implementierung und die ausschließlich
  lesend geprüfte ShopFlow-Debitoren-/Einstellungsreferenz.

### Akzeptanzkriterien

- [x] Fakturieren steht nach der Anmeldung als eigener Hauptnavigationspunkt im Bereich
      Finanzen zur Verfügung und verwendet den aktiven Login-Datenbank-/Benutzerkontext.
- [x] Die Modulshell folgt der bestätigten Debitoren-Hierarchie mit vollständigen
      Aktionsbezeichnungen, Such-/Status-/Schnellansicht-/Filterzeile, stabiler
      Listen-/Detailfläche und verständlichem Leer-, Lade-, Fehler- und deaktiviertem Zustand.
- [x] Das WinUI-eigene Fakturierungsschema wird pro Mandant transaktional, nebenläufig sicher
      und idempotent initialisiert; sein Installationsstand wird von den Repositories geprüft.
- [x] Finanzstammdaten werden vollständig und transaktional persistiert und fachlich
      validiert. Basiswährung ist aktiv; aktive Fremdwährungen besitzen positive, datierte
      manuelle Kurse mit Quelle; QR-Konten sind auf CHF/EUR und zulässige CH-/LI-QR-IBAN
      begrenzt; Nummernkreise, MWST-Sätze und Kontenzuordnungen sind konsistent.
- [x] Adressen, Geldinstitute und Kontenplan werden referenziert statt dupliziert.
      Zahlungs-/QR-Konten verweisen auf vorhandene `dbo.Geldinstitut`-Einträge; Ertrags-
      und Kursdifferenzkonten verweisen auf vorhandene `dbo.Kontenplan`-Einträge.
- [x] Normale Benutzer sehen und laden die Fakturieren-Arbeitsfläche, können jedoch weder den
      Finanzen-Einstellungsbereich öffnen noch dessen Repository-Schreibweg verwenden.
- [x] Es werden keine Passwörter, Tokens oder anderen Geheimnisse ungeschützt in der
      Mandantendatenbank gespeichert. Der Bereich kennzeichnet diese Grenze sichtbar.
- [x] Fremdwährungen sind im Schema, Repository und Einstellungsbereich vorbereitet;
      Swiss-QR wird ausdrücklich nur für CHF/EUR vorgesehen, andere Währungen werden für die
      alternative Zahlungsdarstellung in AP07 gekennzeichnet.
- [x] Der Diff bleibt auf AP02, die WinUI-Produktlinie und dieses Versionsblatt begrenzt;
      AP03- bis AP09-Funktionen werden nicht vorweggenommen.
- [x] Validierungsfehler oder fehlende Eingaben blockieren nicht mehr das Speichern aller
      übrigen Finanzstammdaten. Unabhängige gültige Bereiche werden persistiert,
      fehlerhafte Bereiche und Eingaben bleiben im Formular sichtbar, und die Oberfläche
      unterscheidet klar zwischen vollständig gespeichert und mit Hinweisen gespeichert.
- [x] Der Wechsel aus `Fakturieren` zu `Finanzen` synchronisiert die ausgewählte
      Hauptnavigation mit der Einstellungsseite; ein anschließender Klick auf
      `Fakturieren` führt zuverlässig zurück in das Modul.

### Entscheidungen und Umsetzung

- `Fakturieren` ist im Finanzbereich der Hauptnavigation eingebunden. Die neue
  `InvoicingPage` übernimmt aus ShopFlow Debitoren nur Hierarchie und Bediensemantik:
  Seitenkopf, gruppierte Funktionsleiste, Such-/Status-/Schnellansicht-/Filterfolge,
  Kennzahlen, Listen-/Detailkontext und belastbare Zustände. Die spätere Aktion
  `Neuer Vorgang` bleibt bis zum zuständigen Folgepaket sichtbar deaktiviert.
- Der Sprung von der Fakturieren-Aktion `Finanzen` in den administrativen
  Einstellungsbereich läuft über das Hauptfenster. Dabei werden der ausgewählte
  Hauptnavigationspunkt und die Frame-Seite gemeinsam auf `Einstellungen` gesetzt,
  sodass `Fakturieren` anschließend wieder als Navigationswechsel ausgelöst wird.
- Schema v2 besteht aus acht Fakturierungstabellen mit sieben benannten Fremdschlüsseln,
  drei eindeutigen Indizes, CHF- und Nummernkreis-Seeds. Die Initialisierung verwendet eine
  SQL-Transaktion, `sp_getapplock`, Versions- und Strukturprüfung und verifiziert vor jedem
  Zugriff, dass die geöffnete Datenbank dem Login-Mandanten entspricht.
- `InvoicingWorkspaceRepository` erlaubt angemeldeten Benutzern die normale
  Arbeitsbereichsabfrage. `FinanceSettingsRepository` erzwingt für Laden und Speichern
  zusätzlich die Administratorrolle, validiert vollständig vor dem Schreiben und speichert
  sämtliche Finanzstammdaten in einer serialisierbaren Transaktion.
- Fachliche Validierungsfehler blockieren die Transaktion nicht mehr pauschal.
  Aussteller-/Kontaktdaten, Ländercode, Währungen, Basis-/Kurskonten, Zahlungsziel,
  Nummernkreise, Kurse, MWST, Zahlungs- und Ertragskonten werden über getrennte SQL-Savepoints
  gespeichert. Ein fehlerhafter Bereich wird bis zu seinem Savepoint zurückgerollt, während
  unabhängige Bereiche committed werden. Der Entwurf wird anschließend nicht neu geladen,
  damit fehlerhafte Eingaben zur Korrektur sichtbar bleiben. Verbindungs-, Schema- und
  Commitfehler bleiben notwendigerweise Gesamtfehler.
- Der Einstellungsbereich `Finanzen` führt Ausstellerdaten, die fünf festen
  Dokumentnummernkreise, Basis-/Dokumentwährungen, manuelle Kurse mit Gültigkeit und Quelle,
  MWST, Zahlungs-/QR-Konten, zulässige Ertragskonten und Kursdifferenzkonten. Dichte
  Tabellenbereiche besitzen horizontale, das Gesamtformular eine vertikale
  Scrollalternative.
- Zahlungs-/QR-Konten verweisen auf den bestehenden Geldinstitut-Stamm; Ertrags- und
  Kursdifferenzkonten verweisen weiterhin auf `dbo.Kontenplan`. Ein paralleler
  Geldinstitut-, Konten- oder Adressstamm wird nicht angelegt. Das Schema besitzt keine
  Passwort-, Token- oder Secret-Spalten. Swiss-QR-Konten werden nur für CHF/EUR und
  gültige CH-/LI-QR-IBAN akzeptiert; andere aktive Währungen sind für die alternative
  Zahlungsdarstellung in AP07 gekennzeichnet.
- Name und IBAN eines Zahlungskontos stammen aus dem gewählten Geldinstitut und sind im
  Fakturieren-Bereich nicht als unabhängige Doppelwerte editierbar. Der IBAN-Typ wird
  automatisch ermittelt und sichtbar als `IBAN` oder `QR-IBAN` ausgewiesen; eine
  manuelle, widersprüchliche QR-Kennzeichnung ist nicht mehr möglich.
- Korrektur vom 3. September 2026: Die erste AP02-Fassung bot beim Zahlungskonto
  irrtümlich ein Budgetkonto aus `dbo.Kontenplan` an. AP02 wurde deshalb erneut in
  Umsetzung genommen. Schema v2 migriert die Referenz auf `dbo.Geldinstitut`;
  anhand der IBAN eindeutig erkennbare Altzeilen werden übernommen, nicht zuordenbare
  Altzeilen bleiben erhalten, werden aber bis zur Auswahl eines Geldinstituts deaktiviert.
- Bei kleiner Breite wird der ergänzende Detailkontext zugunsten des zentralen
  Leerzustands ausgeblendet. Alle Informationen bleiben über Kennzahlen, Leerzustand und
  Statuszeile erhalten; bei breiter Darstellung erscheint der Auswahl- und
  Konfigurationskontext zusätzlich.

### Prüfungen

- Das Schema wurde in temporären SQL-Server-Datenbanken sowohl frisch als v2 als auch über
  den v1→v2-Migrationsweg jeweils zweimal nacheinander initialisiert. Geprüft wurden acht
  Tabellen, Geldinstitut-Spalte und -Fremdschlüssel, die gespeicherte Schemaversion sowie
  die idempotente Wiederholung. Eine Altzeile mit eindeutig passender IBAN wurde dem
  Geldinstitut zugeordnet; eine nicht passende Altzeile blieb erhalten und wurde
  deaktiviert. Die frühere `AccountId`-Spalte bleibt in migrierten Datenbanken als
  nullable Altbestand erhalten und wird vom neuen Code nicht mehr verwendet.
- Ein isolierter Validatorlauf mit zehn Fällen prüfte unter anderem IBAN-Prüfsumme,
  QR-IBAN-Abgrenzung, gültige CHF-Basis, fehlenden und vorhandenen Fremdwährungskurs,
  QR-Währungsgrenze sowie nicht angemeldete und nicht administrative Schreibzugriffe:
  alle Fälle erfolgreich.
- Ein Repository-Ende-zu-Ende-Lauf mit dreizehn Prüfungen bestätigte Seed-Laden,
  Kontenplan- und Geldinstitutoptionen, Speichern und erneutes Laden von CHF/EUR,
  aktuellem Kurs, MWST,
  Zahlungs-, Ertrags- und Kursdifferenzkonten, den vollständig konfigurierten
  Arbeitsbereich sowie die Admin-Sperre. Testdatenbank und Testquellen wurden entfernt.
- Ein zusätzlicher Regressionstest bestätigte
  `CH1180808004093838734` mit ISO-13616-Modulo-97 als gültige normale Schweizer IBAN
  und ausdrücklich nicht als QR-IBAN. Der Repository-Lauf speicherte dafür
  `GeldinstitutId = 11` und `IsQrIban = 0`; erneutes Laden lieferte Name und IBAN aus
  `dbo.Geldinstitut`. Testdatenbank und Testquellen wurden entfernt.
- Ein Teil-Speicher-Ende-zu-Ende-Test verwendete vollständige Aussteller-/Adressdaten und
  gleichzeitig ein Geldinstitut ohne IBAN. Das Repository meldete sichtbare Hinweise,
  speicherte Firma, Strasse, PLZ und Ort, ließ das fehlerhafte Zahlungskonto unwirksam und
  behielt dessen Auswahl im Formularentwurf. Nach Auswahl des Geldinstituts mit gültiger
  IBAN wurde derselbe Entwurf vollständig gespeichert. Testdatenbank und Testquellen wurden
  entfernt.
- Die reale Anwendung wurde bis zur Login-Oberfläche gestartet. Ein zusätzlicher,
  anschließend vollständig entfernter UI-Prüfpfad bestätigte die authentifizierte
  AP02-Seite bei 1360 × 860 und 820 × 700 Pixeln: Titel und Leerzustand blieben sichtbar,
  `Neuer Vorgang` war deaktiviert, Aktualisieren und Finanzen waren barrierefrei
  adressierbar, und die Kursdifferenzkonten wurden entlang des vertikalen Scrollwegs
  sichtbar; der Endanschlag war erreichbar.
- XAML-Strukturprüfung bestätigte vollständige Texte ohne `TextTrimming`, horizontale
  Scrollalternativen, adaptive Listen-/Detaildarstellung, Tastaturkürzel und Fokuspfade.
  Beide neuen XAML-Dateien wurden als XBF kompiliert. Eine separate native
  Betriebssystem-Skalierungsstufe wurde nicht umgeschaltet; ihr Layoutdruck ist durch
  Umbruch, Scrollalternativen und die schmale Live-Prüfung abgedeckt.
- Ein isolierter UI-Automationslauf bestätigte den korrigierten Navigationsweg:
  Nach dem Wechsel aus `Fakturieren` zu `Finanzen` war `Einstellungen`
  ausgewählt; der anschließende Klick auf `Fakturieren` wählte den Hauptnavigationspunkt
  und zeigte die Fakturieren-Seite wieder an. Der temporäre Debug-Prüfpfad wurde danach
  vollständig entfernt.
- `dotnet build MyCoinFlow.App.WinUI\MyCoinFlow.App.WinUI.csproj -c Debug --no-restore`:
  erfolgreich mit 0 Fehlern und drei bestehenden NU1701-Warnungen aus dem unveränderten
  referenzierten WPF-Projekt.
- `git diff --check`, finaler Scope-Diff, Git-Status, Prüfung auf temporäre
  UI-/Datenbankreste und Schutz der übernommenen Änderungen: erfolgreich.

### Abschluss

- Commit: `v3.1.1.0 AP02: Fakturieren-Grundlage und Finanzen einstellen`
- Push: `origin/master`
- Ergebnis: Navigation, leerzustandsfähige Modulshell, mandanten- und rollengesicherte
  Schema-/Repository-Grundlage sowie die vollständig persistierten und validierten
  Finanzstammdaten sind umgesetzt. Zahlungskonten wählen ein bestehendes Geldinstitut;
  normale und QR-IBAN werden automatisch unterschieden. Fehlerhafte oder fehlende Eingaben
  verhindern nicht mehr die Speicherung unabhängiger Datenbereiche und bleiben mit
  sichtbaren Hinweisen zur Korrektur im Formular. AP02 greift weder
  Artikel/Positionen noch Dokument-,
  Zahlungs-, PDF-, QR-Ausgabe-, E-Mail- oder DMS-Abläufe der Folgepakete vor.
- Offene Punkte: keine AP02-Blocker. Die Prüfung unter einer abweichenden nativen
  Betriebssystem-Skalierungsstufe bleibt Teil der abschließenden manuellen Releaseabnahme.
  Der noch nicht separat committete Versionsstart und die rein konzeptionelle AP01-
  Dokumentation werden transparent im AP02-Abschlusscommit mitgeführt; ihre fachliche
  Zuordnung bleibt in diesem Versionsblatt getrennt.

## Arbeitspaket 03 · Artikel und fakturierbare Objekte

- Tasktitel: 3.1.1.0 · 03 · Artikel und fakturierbare Objekte
- Status: abgeschlossen
- Ziel: Einen belastbaren, mandantenbezogenen Artikel-/Leistungsstamm und die zwei
  auswählbaren Quellen fakturierbarer Objekte bereitstellen: direkte Sach-/Leistungsobjekte
  aus dem Artikelstamm sowie Liegenschaften/Stockwerkeinheiten aus dem vorhandenen
  Immobilienbestand. Ausschließlich WinUI-eigene Fakturierungsprofile ergänzen die
  bestehenden Adress-, Kontenplan-, STWE-Liegenschafts-, Einheiten-, Eigentümer- und
  Eigentumsdaten um Rechnungsadressreferenzen, datierte Nutzung und belastbare
  Miet-/Nebenkosteninformationen, ohne parallele Adress- oder Immobilien-Stämme anzulegen.
- Nicht-Ziele: Rechnungs- oder Dokumententwürfe, Position-Composer, Textbausteinverwaltung,
  definitive Rechnungen, Akontoerzeugung, Nebenkosten- oder Schlussabrechnung, automatische
  rechtliche Einzelfallentscheidung, automatische Umlage von Reparaturen, Erneuerungen oder
  anderen nicht überwälzbaren Kosten; Änderungen an WPF-XAML, am gemeinsamen Fachkern oder
  an ShopFlow; allgemeiner Umbau des Liegenschaften-Moduls.
- Ausgangsstand: sauberer, nach origin/master gepushter Commit 989db8f auf master mit
  dem Abschluss v3.1.1.0 AP02: Fakturieren-Grundlage und Finanzen einstellen.
- Betroffene Module: MyCoinFlow.App.WinUI/Data/InvoicingSchema.cs für die additive,
  idempotente Schemamigration; neue getrennte Fakturieren-Repositories und Modelle für
  Artikel sowie Objekt-/Empfängerprofile; MyCoinFlow.App.WinUI/ViewModels/InvoicingViewModel.cs
  und MyCoinFlow.App.WinUI/Views/InvoicingPage.* für Einstieg und auswählbare Objekte;
  neue persistente WinUI-Editorfenster/Controls für Artikel- und
  Immobilien-Fakturierungsstammdaten; bestehende
  Immobilienseiten nur, falls eine für Fakturieren zwingende WinUI-Integration nachweislich
  nicht innerhalb des Fakturieren-Moduls lösbar ist.
- Mandanten- und Datengrenze: Alle neuen Tabellen liegen im bestehenden mandantenbezogenen
  Fakturierungsschema und jede Verbindung wird wie in AP02 gegen den aktiven Login-Mandanten
  geprüft. dbo.Adresse, dbo.Kontenplan, dbo.StweLiegenschaft, dbo.StweEinheit,
  dbo.StweEigentuemer und dbo.StweEinheitEigentum werden ausschließlich referenziert.
  Der vorhandene technische Tenant-Begriff bleibt Datenbankmandant und wird nicht als
  Synonym für Mieter verwendet.
- Rechtliche Leitplanke: Eigentümer ist für STWE-Beiträge und bei Selbstnutzung oder
  Leerstand der sichere Standardempfänger. Ein Mieter darf nur bei am Stichtag aktivem
  Mietverhältnis, ausdrücklich dokumentiertem Nebenkostenmodus und dokumentierter
  Direktfakturierungsfreigabe als Rechnungsempfänger gewählt werden; der Eigentümer bleibt
  als verantwortliche Partei sichtbar. Die Oberfläche verweist als Prüfrahmen auf
  Art. 712h ZGB, Art. 257a/257b OR, VMWG und die BWO-Hinweise zur jährlichen
  Akontoabrechnung, zeigt aber keine automatische Rechtsentscheidung. Reparaturen,
  Erneuerungen und als nicht überwälzbar klassifizierte Artikel werden nicht automatisch als
  Mieternebenkosten angeboten.
- ShopFlow-Referenz: Vermietung liefert für Stammdaten die Listen-/Detailgrammatik;
  Debitoren liefert die gruppierte, horizontal scrollbare Funktionsleiste und die
  Such-/Status-/Filterhierarchie. Fachmodelle, Vermietungslogik, Services und Datenzugriffe
  werden nicht kopiert.
- Maßgebliche Quellen: AGENTS.md, MyCoinFlow.App.WinUI/AGENTS.md,
  DOCS/PROJECT_CONTRACT.md, DOCS/ARCHITECTURE.md, DOCS/UX_DESIGN_SYSTEM.md,
  DOCS/UI_ACTION_CATALOG.md, DOCS/UI_CONTRACT_BASELINE.md, DOCS/VERSIONING.md, AP01
  und AP02 dieses Versionsblatts, die aktuelle WinUI-Implementierung und die bestätigten
  fachlichen/rechtlichen Leitplanken dieses Tasks.

### Akzeptanzkriterien

- [x] Artikelnummern werden aus manueller Eingabe deterministisch normalisiert und durch
      Repositoryprüfung sowie einen gross-/kleinschreibungsunabhängigen eindeutigen
      Datenbankindex auch bei Parallelzugriff gegen Dubletten geschützt.
- [x] Artikel führen Bezeichnung, Beschreibung, Einheit, Kategorie, Aktivstatus,
      Verkaufspreis, MWST-Referenz, Ertragskonto-Referenz und eine explizite rechtliche
      Nebenkostenklassifikation; MWST und Ertragskonto verweisen auf vorhandene, zulässige
      Fakturierungsstammdaten beziehungsweise den bestehenden Kontenplan.
- [x] Direkte aktive Artikel/Leistungen und vorhandene Liegenschaften/Einheiten werden als
      getrennte, durchsuchbare Quellen fakturierbarer Objekte geladen und sind für AP04 bis
      AP06 über stabile IDs und einen sichtbaren Fachkontext auswählbar.
- [x] Eigentümer-Fakturierungsprofile referenzieren vorhandene Eigentümer und Adressen;
      datierte Nutzungen referenzieren vorhandene Einheiten; Mietverhältnisse referenzieren
      vorhandene Mieteradressen. Es entsteht kein doppelter Adress-, Eigentümer- oder
      Immobilien-Stamm.
- [x] Nutzung und Mietverhältnis verhindern ungültige Datumsbereiche sowie zeitliche
      Überschneidungen je Einheit. Vermietete Nutzung verlangt ein passendes Mietverhältnis;
      Selbstnutzung und Leerstand lassen keine Mieter-Direktfakturierung zu.
- [x] Eine Mieteradresse wird nur bei aktivem Mietverhältnis, dokumentiertem
      Nebenkostenmodus Akonto oder Pauschale, Vertragsreferenz und ausdrücklicher
      Direktfakturierungsfreigabe auswählbar. Eigentümer und dessen Rechnungsadresse bleiben
      im resultierenden Objektkontext sichtbar; sichere Defaults wählen sonst den Eigentümer.
- [x] Artikel der Klassen Reparatur, Erneuerung oder nicht überwälzbar werden im
      immobilienbezogenen Mieter-Kontext nicht automatisch angeboten; die UI kennzeichnet
      rechtliche Hinweise und den notwendigen manuellen Prüfentscheid verständlich.
- [x] Die Fakturieren-Funktionsleiste besitzt einen vollständig beschrifteten
      Stammdaten-Einstieg. Die Arbeitsfläche folgt einer Listen-/Detailgrammatik und hält bei
      Immobilien die Kette Liegenschaft → Einheit → Zeitraum/Nutzung → verantwortliche
      Partei → Rechnungsempfänger gleichzeitig sichtbar.
- [x] Die mehrteiligen Artikel- und Einheiteneditoren sind frei verschiebbare und
      skalierbare `PersistentWindow`-Fenster. Position und Größe werden gespeichert;
      thematische Karten nutzen die Breite, stapeln bei kleiner Breite ohne Feldverlust und
      halten Kopfkontext sowie Speichern/Abbrechen sichtbar.
- [x] Normale und schmale Breite, Scroll-Fallback, vollständige Texte, Tastaturfokus sowie
      Lade-, Leer-, Auswahl-, Fehler- und deaktivierte Zustände sind strukturell und soweit
      automatisierbar in der laufenden WinUI-App geprüft.
- [x] Frische Installation, Migration von Schema v2 und wiederholte Initialisierung ergeben
      dasselbe vollständige Schema. Repositorytests decken Normalisierung/Dubletten,
      Fremdschlüssel, Zeitüberschneidungen und vollständige Mieter-Direktfakturierung ab.
- [x] Der WinUI-Debug-Build, git diff --check, vollständiger Scope-Diff und Git-Status sind
      dokumentiert; Commit und Push erfolgen erst nach ausdrücklichem Abschlussauftrag.

### Entscheidungen und Umsetzung

- Schema v3 ergänzt ausschließlich das WinUI-Fakturierungsschema um Artikel/Leistungen,
  Eigentümer-Rechnungsprofile, datierte Einheitennutzungen und datierte Mietverhältnisse.
  Die Tabellen referenzieren `dbo.FakturierungMwstSatz`, zugelassene
  `dbo.FakturierungErtragskonto`-Zeilen sowie die vorhandenen Adress-, Kontenplan- und
  STWE-Schlüssel. Die bestehende STWE-Initialisierung wird wiederverwendet; es entsteht kein
  paralleles Liegenschaften-, Eigentümer-, Mieter- oder Adressmodell.
- Artikelnummern werden vor der Validierung Unicode-kompatibel normalisiert, interne
  Leerraumfolgen reduziert und invariant gross geschrieben. Eine serialisierbare
  Repositoryprüfung und der gross-/kleinschreibungsunabhängige eindeutige Datenbankindex
  sichern auch aktive und inaktive Datensätze gegen Dubletten. MWST und das zugelassene
  Ertragskonto werden innerhalb derselben Transaktion erneut geprüft.
- Nutzung und Mietverhältnis besitzen Datenbank-Checks und je Einheit einen Trigger gegen
  überlappende Zeiträume. Das Repository speichert Eigentümerprofil, Mietverhältnis und
  Nutzung atomar, prüft den am Beginn eindeutig zugeordneten Eigentümer und entfernt den
  Mietkontext beim Wechsel zu Selbstnutzung oder Leerstand. Mieter-Direktfakturierung setzt
  Akonto oder Pauschale, Vertragsreferenz, ausdrückliche Freigabe und dokumentierten
  Freigabenachweis voraus.
- Die Abnahme am 3. September 2026 hat die zunächst verwendeten linearen `ContentDialog`-
  Editoren als AP03-Defekt zurückgewiesen: Fenster waren weder frei verschiebbar noch
  skalierbar und konnten Eingabefelder abschneiden. Beide Editoren wurden deshalb vor dem
  AP03-Abschluss durch `PersistentWindow`-Fenster mit gespeicherter Position/Größe,
  thematischen Karten, adaptiver Stapelung, festem Kopf und festen Abschlussaktionen ersetzt.
  Der verbindliche Editorfenster-Standard und seine gemeinsamen WinUI-Styles stehen zentral
  im UX-System, in der WinUI-AGENTS.md und in App.xaml.
- Die neue Stammdatenoberfläche verwendet vorhandene MWST-Sätze, Adressen, Eigentümer,
  Einheiten und den gemeinsamen Kontoeditor. Das Anlegen und Zulassen eines neuen
  Ertragskontos wahrt die AP02-Administratorgrenze im UI und Repository. Artikel- und
  Nutzungseditorfenster zeigen Validierungsfehler im Kontext und halten bedingte Mietfelder bei
  Selbstnutzung oder Leerstand deaktiviert.
- Die Fakturieren-Seite lädt direkte aktive Artikel/Leistungen und Immobilienkontexte als
  getrennte Quellen mit stabilen Schlüsseln. Immobilien zeigen die vollständige Kette
  Liegenschaft → Einheit → Zeitraum/Nutzung → verantwortliche Partei →
  Rechnungsempfänger. Eigentümer ist immer der vorausgewählte sichere Empfänger; die
  Mieteradresse erscheint nur als ausdrücklich bezeichnete Alternative, wenn alle
  dokumentierten Voraussetzungen am Stichtag erfüllt sind.
- ShopFlow liefert ausschließlich die erkennbare Funktionsleisten-, Filter-, Listen- und
  Detailgrammatik. Fachmodell, Vermietungslogik, Services und Datenzugriffe wurden nicht
  übernommen. Rechnungs-/Dokumententwürfe, Positionen, automatische Akonto- oder
  Nebenkostenabrechnungen und rechtliche Einzelfallentscheide bleiben außerhalb AP03.
- Auf ausdrücklichen Benutzerauftrag wurden begleitend sämtliche im Repository vorhandenen
  Spuren eines früheren externen Entwicklungswerkzeugs entfernt. Die einzige Änderung unter
  `MyCoinFlow/` betrifft dabei die neutrale Formulierung einer portablen DMS-Dokumentation;
  Fachkern und WPF-XAML bleiben unverändert. Lokale Binärdatei und Einstellungen waren nicht
  versioniert.

### Prüfungen

- Ein isolierter SQL-Ende-zu-Ende-Lauf initialisierte Schema v3 frisch und wiederholt sowie
  nach einem kontrollierten Rückbau auf v2 erneut zweimal. Geprüft wurden Version 3,
  zwölf Fakturierungstabellen, vierzehn benannte Fremdschlüssel, vier eindeutige Indizes und
  zwei Zeitraumtrigger. Testdatenbank und temporäre Testquellen wurden danach entfernt.
- Repository- und Datenbankfälle bestätigten Artikelpersistenz, Unicode-/Leerraum-/
  Grossschreibungsnormalisierung, Repository- und Index-Dublettenabwehr, aktive MWST,
  zugelassenes Ertragskonto, Fremdschlüsselablehnung eines nur im Kontenplan vorhandenen
  Ertragskontos, gültige und überlappende Nutzungs-/Mietzeiträume sowie die vollständige
  Mieter-Direktfakturierungsprüfung. Ein zusätzlicher Lauf bestätigte die Nicht-Admin-Sperre
  der Ertragskonto-Zulassung.
- Der resultierende Objektarbeitsbereich wurde mit direkten Artikeln und einer vermieteten
  Einheit geprüft: stabile Quellen-/Objekt-IDs, Suche, auswählbarer Eigentümer-Standard,
  sichtbare verantwortliche Partei und nur bei vollständig dokumentiertem Mietkontext
  verfügbare Mieter-Alternative waren erfolgreich.
- Die reale WinUI-App wurde über einen anschließend vollständig entfernten isolierten
  Debug-Prüfpfad mit Testdaten bei 1360 × 860 und 820 × 700 Pixeln abgenommen.
  Hauptarbeitsfläche und Stammdatenfenster zeigten vollständig beschriftete Aktionen,
  horizontale Filter-/Funktionsleisten-Fallbacks, gestapelte Listen-/Detailkarten,
  Objektkette, Eigentümer-Vorgabe und bewusst wählbare Mieter-Alternative. UI Automation
  bestätigte Suche, Auswahl, Tastatur-/Aktionszustände, den Pflichtfeldfehler des leeren
  Artikeleditors und deaktivierte Mietfelder bei `Selbstbewohnt`.
- Nach der Korrektur wurden beide echten Editorfenster live in ihrer breiten
  Standarddarstellung und bei 820 × 700 Pixeln geprüft. Artikelgrunddaten,
  Verkauf/Kontierung, Sortimentsstatus, Zeitraum/Nutzung, Eigentümer und Mietkontext sind in
  benannten Karten gegliedert; die schmale Darstellung stapelt sie. Größenänderung,
  vollständige MWST- und Datumsdarstellung, sichtbare Abschlussaktionen sowie die durch
  `PersistentWindow` gespeicherten Standardgrößen 1180 × 800 beziehungsweise 1220 × 840
  wurden bestätigt. Der isolierte Vorschau-Einstieg wurde danach vollständig entfernt.
- XAML-Strukturprüfung bestätigte Umbruch statt `TextTrimming`, vertikale und horizontale
  Scrollalternativen, Fokuspfade und zugängliche Namen. Eine separate native
  Betriebssystem-Skalierungsstufe wurde nicht umgeschaltet; deren zusätzlicher Layoutdruck
  bleibt Teil der manuellen Releaseabnahme und ist strukturell durch Umbruch,
  Scroll-Fallbacks und die schmale Live-Prüfung abgedeckt.
- `dotnet build MyCoinFlow.App.WinUI\MyCoinFlow.App.WinUI.csproj -c Debug --no-restore`:
  erfolgreich mit 0 Fehlern und drei bestehenden NU1701-Warnungen aus dem unveränderten
  referenzierten WPF-Projekt.
- `git diff --check`, vollständiger Scope-Diff, Git-Status sowie Prüfung auf temporäre
  UI-, Testhost- und Datenbankreste: erfolgreich. Eine vollständige Suche nach Text- und
  Dateinamensspuren des entfernten Werkzeugs ergab keine verbleibenden Treffer.

### Abschluss

- Commit: `v3.1.1.0 AP03: Artikel und fakturierbare Objekte`
- Push: `origin/master`
- Ergebnis: Artikel-/Leistungsstamm, zugelassene artikelabhängige Ertragskonten,
  datierte Immobilien-/Nutzungs-/Empfängerprofile und der durchsuchbare Arbeitsbereich für
  direkte und immobilienbezogene fakturierbare Objekte sind umgesetzt. Rechtliche
  Leitplanken bleiben sichtbar und führen zu keiner automatischen Einzelfallentscheidung.
- Offene Punkte: keine AP03-Blocker. Die Abnahme unter einer abweichenden nativen
  Betriebssystem-Skalierungsstufe bleibt Bestandteil der späteren manuellen Releaseabnahme;
  AP04 übernimmt erst nach eigenem Startauftrag Positionen und Dokumententwürfe.

### Parkplatz

- Automatische Erzeugung von Akontorechnungen, jährliche Nebenkostenabrechnung und
  Schlussrechnung bleiben AP04 bis AP06 beziehungsweise einem daraus klar abgegrenzten
  Folgepaket vorbehalten.
- Eigentumszeiträume des bestehenden STWE-Fachkerns werden in AP03 gelesen und gegen den
  Fakturierungsstichtag plausibilisiert; eine allgemeine Bereinigung oder Neugestaltung der
  bestehenden Eigentumsverwaltung bleibt außerhalb dieses Arbeitspakets.

## Parkplatz

- Automatische externe Wechselkursquellen können nach einem funktionierenden manuellen
  Kursstamm als eigenes Folgepaket bewertet werden; Fremdwährungsfähigkeit selbst gehört
  bereits zum bestätigten Umfang.
- Unabhängige Angleichungen anderer WinUI-Module bleiben außerhalb dieser Version, sofern
  sie nicht als zwingende gemeinsame Grundlage ausdrücklich bestätigt werden.
