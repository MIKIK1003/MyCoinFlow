# MyCoinFlow 3.1.1.1

- Status: Klärung
- Basisversion: 3.1.1.0
- Basiscommit: ddc94545b2bc2500ce054ec57ccf4d36f4944b03
- Start: 2026-09-04
- Versionszweig: master
- Versionsziel: Die vom Benutzer noch zu benennenden gezielten Finanzkorrekturen fachlich
  klären, auf nachvollziehbare Einzeländerungen begrenzen und erst nach Bestätigung in der
  produktiven WinUI-Anwendung umsetzen.

Der Basiscommit `ddc94545b2bc2500ce054ec57ccf4d36f4944b03` ist der auf `origin/master`
veröffentlichte Abschluss von 3.1.1.0 AP09. `HEAD` und `origin/master` standen beim
Versionsstart auf diesem Commit; der Arbeitsbaum war sauber. Die frühere WPF-Oberfläche
unter `MyCoinFlow/` und ShopFlow sind nicht Teil dieses Versionsstarts.

## Startprüfung

- Version 3.1.1.0 ist im vorherigen Versionsblatt mit AP01 bis AP09 abgeschlossen; AP08 und
  AP09 wurden gemeinsam unter `v3.1.1.0 AP09: Fakturierung abschließen` veröffentlicht.
- `MyCoinFlow.App.WinUI/MyCoinFlow.App.WinUI.csproj` stand einheitlich mit `Version`,
  `AssemblyVersion` und `FileVersion` auf 3.1.1.0 und wird für diesen Abschnitt einheitlich
  auf 3.1.1.1 erhöht. Die frühere WPF-Version bleibt unverändert.
- Es liegen noch keine konkreten Fehlerbilder, Sollabläufe, betroffenen Finanzbereiche oder
  Akzeptanzfälle für die angekündigten Korrekturen vor. Deshalb beginnt AP01 in Klärung und
  enthält keine vorweggenommene Fach-, Daten- oder UI-Implementierung.

## Arbeitspaket 01 · Gezielte Finanzkorrekturen

- Tasktitel: 3.1.1.1 · AP01 · Gezielte Finanzkorrekturen
- Status: Klärung
- Ziel: Die noch folgenden Änderungswünsche des Benutzers je Korrektur als beobachtbares
  Ist-/Soll-Verhalten erfassen, gegen die aktuelle WinUI-Implementierung und die
  Finanzinvarianten prüfen und in einen bestätigten, umsetzbaren Umfang überführen.
- Nicht-Ziele: Vor den konkreten Änderungswünschen fachliche Fehler, gewünschte Abläufe oder
  betroffene Module annehmen; Finanz-, Buchungs-, Zahlungs-, Mandanten-, Berechtigungs-,
  Lizenz- oder Revisionslogik vorsorglich ändern; WPF-XAML, ShopFlow oder unabhängige
  WinUI-Bereiche bearbeiten; weitere Arbeitspakete vorwegnehmen.
- Ausgangsstand: sauberer, mit `origin/master` synchroner Commit
  `ddc94545b2bc2500ce054ec57ccf4d36f4944b03` auf `master`; ausschließlich die
  WinUI-Projektversion, dieses Versionsblatt und der aktive README-Verweis bilden den
  Versionsstartrahmen.
- Betroffene Module: Bis zur fachlichen Spezifikation schreibend nur
  `MyCoinFlow.App.WinUI/MyCoinFlow.App.WinUI.csproj`, dieses Versionsblatt und `README.md`.
  Konkrete WinUI-Views, ViewModels, Repositories, Services, Modelle und Datenflüsse werden
  erst nach Prüfung der Änderungswünsche benannt.
- Maßgebliche Quellen: `AGENTS.md`, `MyCoinFlow.App.WinUI/AGENTS.md`,
  `DOCS/PROJECT_CONTRACT.md`, `DOCS/ARCHITECTURE.md`, `DOCS/VERSIONING.md`, das
  abgeschlossene Versionsblatt `DOCS/VERSIONS/3.1.1.0.md`, die aktuelle
  WinUI-Implementierung und die noch ausstehenden Änderungswünsche des Benutzers. Bei
  konkreten UI-Arbeiten gelten zusätzlich `DOCS/UX_DESIGN_SYSTEM.md`,
  `DOCS/UI_ACTION_CATALOG.md` und `DOCS/UI_CONTRACT_BASELINE.md`.

### Akzeptanzkriterien

- [ ] Jede gewünschte Finanzkorrektur ist mit aktuellem Verhalten, erwartetem Verhalten,
      betroffenen Daten beziehungsweise Abläufen und bewusstem Nicht-Ziel bestätigt.
- [ ] Betroffene WinUI-Module und bestehende Implementierungswege sind im aktuellen Code
      geprüft und im Versionsblatt benannt; Dokumentation ersetzt nicht die Codeprüfung.
- [ ] Mandantentrennung, Datenintegrität, Buchungs- und Transaktionsgrenzen,
      Berechtigungen, Lizenzierung und revisionsrelevante Nachweise sind je Korrektur als
      unverändert oder ausdrücklich betroffen dokumentiert.
- [ ] Für jede bestätigte Korrektur bestehen überprüfbare Akzeptanzfälle und ein
      risikogerechter Prüfplan, bevor Fachcode, Datenzugriff oder UI geändert wird.
- [ ] Der Versionsstart enthält keine erfundene oder vorweggenommene Finanzkorrektur und
      keine Änderung an WPF-XAML oder ShopFlow.

### Entscheidungen und Umsetzung

- Noch nicht fachlich spezifiziert. Die Umsetzung beginnt erst nach den konkreten
  Änderungswünschen und ihrer Prüfung gegen den aktuellen Code.

### Geplante Prüfungen

- Nach Eingang der Änderungswünsche die betroffenen Code-, XAML-, Daten- und
  Transaktionspfade sowie die geltenden Verträge gezielt prüfen.
- Akzeptanzfälle und relevante Builds beziehungsweise Tests erst aus dem bestätigten
  Korrekturumfang ableiten.
- Vor jedem Abschluss `git diff --check`, vollständigen Scope-Diff, Git-Status und eine
  Suche nach temporären Prüfartefakten ausführen.

### Versionsstart-Prüfungen

- Die Projektdatei wurde als XML gelesen; `Version`, `AssemblyVersion` und
  `FileVersion` stehen einheitlich auf 3.1.1.1.
- `git diff --check` war erfolgreich; der Scope-Diff ist auf Projektversion, README und das
  neue Versionsblatt begrenzt.
- `HEAD` und `origin/master` stehen weiterhin auf
  `ddc94545b2bc2500ce054ec57ccf4d36f4944b03`. Für diese reine Versions- und
  Dokumentationsänderung wurde kein Build ausgeführt.

### Zwischenstände

- 2026-09-04: Reiner Versionsstartrahmen aus WinUI-Projektversion, aktivem README-Verweis
  und diesem Versionsblatt unter `v3.1.1.1 AP01: Versionsstart zwischensichern`
  zwischengesichert und auf `origin/master` veröffentlicht. AP01 bleibt in Klärung; die
  Zwischensicherung enthält keine fachliche Finanzkorrektur und ist kein Abschluss.

### Abschluss

- Abschlusscommit: noch keiner
- Abschlusspush: noch keiner
- Offene Punkte: Konkrete Änderungswünsche, betroffene Finanzbereiche, Sollverhalten und
  Abnahmekriterien stehen noch aus.

## Parkplatz

- Noch leer.
